% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_df_to_delta_utils.R
\name{resolve_table_parts}
\alias{resolve_table_parts}
\title{Resolve Table Name into Catalog, Schema, and Table}
\usage{
resolve_table_parts(table_name, db_conn)
}
\arguments{
\item{table_name}{A character string specifying the table name.
Can be one of the following:
\itemize{
\item \code{"table"} (unqualified)
\item \code{"schema.table"} (partially qualified)
\item \code{"catalog.schema.table"} (fully qualified)
}}

\item{db_conn}{A DBI database connection object (e.g., created using
\code{DBI::dbConnect()}).}
}
\value{
A named list with components:
\describe{
\item{\code{catalog}}{The catalog name, either extracted from the input or
resolved from the session via \code{current_catalog()}.}
\item{\code{schema}}{The schema name, either extracted from the input or
resolved from the session via \code{current_schema()}.}
\item{\code{table}}{The bare table name.}
}
}
\description{
Resolves a table name into its fully qualified components—catalog, schema,
and table—using the current session defaults from the Databricks connection
if necessary.
}
\examples{
\dontrun{
conn <- DBI::dbConnect(odbc::odbc(), "Databricks")
resolve_table_parts("my_table", conn)
resolve_table_parts("my_schema.my_table", conn)
resolve_table_parts("my_catalog.my_schema.my_table", conn)
}
}
\keyword{internal}
