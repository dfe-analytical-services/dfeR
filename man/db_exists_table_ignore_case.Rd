% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_df_to_delta_utils.R
\name{db_exists_table_ignore_case}
\alias{db_exists_table_ignore_case}
\title{Check if a Table Exists in Databricks Ignoring Case}
\usage{
db_exists_table_ignore_case(db_conn, table_name)
}
\arguments{
\item{db_conn}{A DBI-compatible database connection object connected to
Databricks.}

\item{table_name}{A character string specifying the table name to check.
Can be unqualified (\code{"table"}), partially qualified (\code{"schema.table"}), or
fully qualified (\code{"catalog.schema.table"}).}
}
\value{
Logical \code{TRUE} if the table exists (case-insensitive match),
otherwise \code{FALSE}.
}
\description{
Performs a case-insensitive existence check for a Delta table in Databricks
by querying the \code{information_schema.tables} metadata. This function splits
the provided table name into catalog, schema, and table parts, resolves
defaults from the current session, and compares lowercased identifiers to
avoid issues with case sensitivity.
}
\details{
\itemize{
\item The function uses \code{resolve_table_parts()} to split the table name and fill
in missing catalog or schema using the current connection context.
\item It queries the \code{information_schema.tables} in the resolved catalog and
compares \code{table_schema} and \code{table_name} in lowercase to handle case
insensitivity.
\item If the metadata query fails, a warning is issued, and the function
returns \code{FALSE}.
}
}
\examples{
\dontrun{
db_exists_table_ignore_case(db_conn, "my_table")
db_exists_table_ignore_case(db_conn, "my_schema.my_table")
db_exists_table_ignore_case(db_conn, "my_catalog.my_schema.my_table")
}
}
\keyword{internal}
