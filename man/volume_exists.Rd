% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_df_to_delta_utils.R
\name{volume_exists}
\alias{volume_exists}
\title{Check if a Databricks Volume Exists}
\usage{
volume_exists(volume_dir)
}
\arguments{
\item{volume_dir}{Character. The full path to the Databricks volume in the
format \verb{/Volumes/catalog/schema/volume/...}. Only the first three path
components after \verb{/Volumes/} are used to identify the volume.}
}
\value{
Logical. \code{TRUE} if the volume exists, \code{FALSE} if the volume does not
exist. Throws an error for unexpected HTTP statuses or missing environment
variables.
}
\description{
This function checks whether a Databricks Unity Catalog volume exists.
It converts a standard volume path (e.g., \verb{/Volumes/catalog/schema/volume})
to the fully qualified volume name (\code{catalog.schema.volume}) required by the
Databricks REST API and performs a GET request. Transient connection issues
are automatically retried using \code{safe_req_perform()}.
}
\details{
The function requires the environment variables \code{DATABRICKS_HOST} and
\code{DATABRICKS_TOKEN} to be set for authenticating API requests. A 404 response
is interpreted as "volume does not exist", while other HTTP statuses
(e.g., 500, 403) will cause an error.
}
\examples{
\dontrun{
volume_exists("/Volumes/my_catalog/my_schema/my_volume")
}

}
\keyword{internal}
