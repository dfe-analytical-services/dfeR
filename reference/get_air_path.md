# Get the expected Air executable path for the current platform

works out the platform-specific Air executable name and expected install
location (in `~/.local/bin/`), returning both alongside the resolved
home directory and full path to the executable.

## Usage

``` r
get_air_path()
```
