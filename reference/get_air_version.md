# Get the installed Air version

returns the installed Air version as a
[`numeric_version()`](https://rdrr.io/r/base/numeric_version.html), or
`NA` if Air is not installed or its version cannot be determined.

## Usage

``` r
get_air_version(air_path)
```

## Arguments

- air_path:

  path to the air executable
